{
  "name": "agent-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Agent Studio — MCP (Model Context Protocol) server descriptor + client contract.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
