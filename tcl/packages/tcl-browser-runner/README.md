# @protectqa/tcl-browser-runner

Static dashboard for TCL live findings (SSE / poll against ProtectQA Agent Studio API).

## Dev

```bash
npm run dev
```

Open http://localhost:5174 — set API base, team id, and Supabase access token.

## Publish

Built assets ship in `dist/` after `npm run build`.
