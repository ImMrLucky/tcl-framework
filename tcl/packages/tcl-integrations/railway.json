{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd packages/tcl-integrations && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd packages/tcl-integrations && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

