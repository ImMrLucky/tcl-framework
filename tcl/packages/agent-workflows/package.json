{
  "name": "agent-workflows",
  "version": "0.1.0",
  "private": true,
  "description": "Agent Studio — workflow templates and state machine contracts (generic delivery, BMAD Workflow Pack, Bug Fix Flow, etc.).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
